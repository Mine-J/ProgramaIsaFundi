name: Reservas Deportes

on:
  schedule:
    # Lunes
    - cron: "45 12 * * 6"  # 16:30 Madrid -45min → 13:45 UTC
    - cron: "45 13 * * 6"  # 17:30 Madrid -45min → 14:45 UTC
    - cron: "45 14 * * 6"  # 18:30 Madrid -45min → 15:45 UTC
    # Martes
    - cron: "0 13 * * 0"   # 15:45 Madrid -45min → 13:00 UTC
    - cron: "45 14 * * 0"  # 17:30 Madrid -45min → 14:45 UTC
    # Miércoles
    - cron: "45 12 * * 1"  # 16:30 Madrid -45min → 13:45 UTC
    - cron: "45 13 * * 1"  # 17:30 Madrid -45min → 14:45 UTC
    - cron: "45 14 * * 1"  # 18:30 Madrid -45min → 15:45 UTC
    # Jueves
    - cron: "0 12 * * 2"   # 15:45 Madrid -45min → 13:00 UTC
    - cron: "45 13 * * 2"  # 17:30 Madrid -45min → 14:45 UTC
    # Viernes
    - cron: "45 11 * * 3"  # 15:30 Madrid -45min → 12:45 UTC
    - cron: "45 12 * * 3"  # 16:30 Madrid -45min → 13:45 UTC

  # Permite lanzarlo manualmente desde GitHub (Actions → Run workflow)
  workflow_dispatch:

jobs:
  run-script:
    runs-on: ubuntu-latest
    env:
      TZ: Europe/Madrid # 🔹 Fuerza la zona horaria a Madrid
      MONGO_URL: ${{ secrets.MONGO_URL }}
      EMAIL: ${{ secrets.EMAIL }}
      PASSWORD: ${{ secrets.PASSWORD }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          playwright install chromium

      - name: Debug env
        run: |
          echo "EMAIL length: ${#EMAIL}"
          echo "PASSWORD length: ${#PASSWORD}"
          echo "MONGO_URL length: ${#MONGO_URL}"

      - name: Run script
        run: python ProgramaFundi.py

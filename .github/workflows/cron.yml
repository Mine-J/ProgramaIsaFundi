name: Reservas Deportes

on:
  schedule:
    # Lunes
    - cron: "20 13 * * 6"
    - cron: "35 14 * * 6"
    - cron: "35 15 * * 6"

    # Martes
    - cron: "20 13 * * 0"
    - cron: "35 15 * * 0"

    # Miércoles
    - cron: "20 13 * * 1"
    - cron: "35 14 * * 1"
    - cron: "35 15 * * 1"

    # Jueves
    - cron: "20 13 * * 2"

    # Viernes
    - cron: "25 12 * * 3"
    - cron: "25 13 * * 3"

  workflow_dispatch:

jobs:
  run-script:
    runs-on: ubuntu-latest

    env:
      TZ: Europe/Madrid
      EMAIL: ${{ secrets.EMAIL }}
      PASSWORD: ${{ secrets.PASSWORD }}
      MONGO_URL: ${{ secrets.MONGO_URL }}
      PERSON_CODE: ${{ secrets.PERSON_CODE }}
      NOMBRE: ${{ secrets.NOMBRE }}
      APELLIDOS: ${{ secrets.APELLIDOS }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pymongo motor
          playwright install chromium

      - name: Debug env
        run: |
          echo "EMAIL length: ${#EMAIL}"
          echo "PASSWORD length: ${#PASSWORD}"
          echo "MONGO_URL length: ${#MONGO_URL}"
          echo "PERSON_CODE length: ${#PERSON_CODE}"
          echo "NOMBRE length: ${#NOMBRE}"
          echo "APELLIDOS length: ${#APELLIDOS}"

      - name: Run script
        run: python ProgramaFundi.py
